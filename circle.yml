dependencies:
    pre:
        - sudo apt-get update; sudo apt-get install libpcap --no-install-recommends
        - go get github.com/tools/godep
test:
    override:
        - godep go test -cover -v ./...
